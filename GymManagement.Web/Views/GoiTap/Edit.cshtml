@model GymManagement.Web.Models.DTOs.UpdateGoiTapDto
@{
    ViewData["Title"] = "Chỉnh sửa gói tập";
    Layout = "_Layout";
}

<div class="p-6">
    <!-- Simple Header -->
    <div class="mb-6">
        <div class="flex items-center gap-4 mb-4">
            <a href="@Url.Action("Index")" class="text-gray-600 hover:text-gray-800 transition-colors">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                </svg>
            </a>
            <div>
                <h1 class="text-2xl font-bold text-gray-900">Chỉnh sửa gói tập</h1>
                <p class="text-gray-600 text-sm">Cập nhật thông tin gói tập: <span class="font-semibold">@Model.TenGoi</span></p>
            </div>
        </div>
    </div>

        <!-- Form Card with Modern Design -->
        <div class="max-w-4xl mx-auto">
            <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
                <!-- Card Header -->
                <div class="bg-gray-50 px-8 py-6 border-b border-gray-200">
                    <div class="flex items-center space-x-3">
                        <div class="bg-blue-100 rounded-xl p-3">
                            <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                            </svg>
                        </div>
                        <div>
                            <h2 class="text-2xl font-bold text-gray-900">Cập nhật thông tin gói tập</h2>
                            <p class="text-gray-600 mt-1">Chỉnh sửa các thông tin cần thiết và lưu thay đổi</p>
                        </div>
                    </div>
                </div>

                <!-- Form Content -->
                <form asp-action="Edit" method="post" class="p-8">
                    @Html.AntiForgeryToken()
                    <input asp-for="GoiTapId" type="hidden" />

                    <!-- Enhanced Validation Summary -->
                    <div asp-validation-summary="ModelOnly" class="mb-8 p-4 bg-red-50 border-l-4 border-red-400 rounded-r-lg">
                        <div class="flex items-center space-x-3">
                            <svg class="w-5 h-5 text-red-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
                            </svg>
                            <div>
                                <h3 class="text-red-800 font-semibold">Có lỗi xảy ra</h3>
                                <p class="text-red-700 text-sm">Vui lòng kiểm tra lại thông tin và thử lại</p>
                            </div>
                        </div>
                    </div>

            <!-- Package Name -->
            <div class="mb-6">
                <label asp-for="TenGoi" class="block text-sm font-medium text-gray-700 mb-2">
                    Tên gói tập <span class="text-red-500">*</span>
                </label>
                <input asp-for="TenGoi" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                       placeholder="Nhập tên gói tập...">
                <span asp-validation-for="TenGoi" class="text-red-500 text-sm"></span>
            </div>

            <!-- Duration and Sessions Row -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                <!-- Duration -->
                <div>
                    <label asp-for="ThoiHanThang" class="block text-sm font-medium text-gray-700 mb-2">
                        Thời hạn (tháng) <span class="text-red-500">*</span>
                    </label>
                    <input asp-for="ThoiHanThang" type="number" min="1" max="60"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                           placeholder="Nhập số tháng...">
                    <span asp-validation-for="ThoiHanThang" class="text-red-500 text-sm"></span>
                </div>

                <!-- Max Sessions -->
                <div>
                    <label asp-for="SoBuoiToiDa" class="block text-sm font-medium text-gray-700 mb-2">
                        Số buổi tối đa
                    </label>
                    <input asp-for="SoBuoiToiDa" type="number" min="0"
                           class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                           placeholder="Để trống nếu không giới hạn">
                    <span asp-validation-for="SoBuoiToiDa" class="text-red-500 text-sm"></span>
                    <p class="text-xs text-gray-500 mt-1">Để trống nếu không giới hạn số buổi</p>
                </div>
            </div>

            <!-- Price -->
            <div class="mb-6">
                <label asp-for="Gia" class="block text-sm font-medium text-gray-700 mb-2">
                    Giá (VNĐ) <span class="text-red-500">*</span>
                </label>
                <div class="relative">
                    <input asp-for="Gia" type="number" min="0" step="1000"
                           class="w-full px-3 py-2 pr-12 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                           placeholder="Nhập giá gói tập...">
                    <div class="absolute inset-y-0 right-0 flex items-center pr-3 pointer-events-none">
                        <span class="text-gray-500 text-sm">VNĐ</span>
                    </div>
                </div>
                <span asp-validation-for="Gia" class="text-red-500 text-sm"></span>
            </div>

            <!-- Package Type and Status Row -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                <!-- Package Type -->
                <div>
                    <label asp-for="LoaiGoi" class="block text-sm font-medium text-gray-700 mb-2">
                        Loại gói
                    </label>
                    <select asp-for="LoaiGoi" 
                            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                        <option value="">-- Chọn loại gói --</option>
                        <option value="BASIC">Cơ bản</option>
                        <option value="PREMIUM">Cao cấp</option>
                        <option value="VIP">VIP</option>
                        <option value="STUDENT">Sinh viên</option>
                        <option value="FAMILY">Gia đình</option>
                    </select>
                    <span asp-validation-for="LoaiGoi" class="text-red-500 text-sm"></span>
                </div>

                <!-- Status -->
                <div>
                    <label asp-for="TrangThai" class="block text-sm font-medium text-gray-700 mb-2">
                        Trạng thái
                    </label>
                    <select asp-for="TrangThai" 
                            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent">
                        <option value="ACTIVE">Hoạt động</option>
                        <option value="INACTIVE">Tạm dừng</option>
                    </select>
                    <span asp-validation-for="TrangThai" class="text-red-500 text-sm"></span>
                </div>
            </div>

            <!-- Description -->
            <div class="mb-6">
                <label asp-for="MoTa" class="block text-sm font-medium text-gray-700 mb-2">
                    Mô tả gói tập
                </label>
                <textarea asp-for="MoTa" rows="4"
                          class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                          placeholder="Nhập mô tả chi tiết về gói tập..."></textarea>
                <span asp-validation-for="MoTa" class="text-red-500 text-sm"></span>
                <p class="text-xs text-gray-500 mt-1">Tối đa 500 ký tự</p>
            </div>

            <!-- Special Offer -->
            <div class="mb-6">
                <label asp-for="UuDaiDacBiet" class="block text-sm font-medium text-gray-700 mb-2">
                    Ưu đãi đặc biệt
                </label>
                <input asp-for="UuDaiDacBiet" 
                       class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                       placeholder="Ví dụ: Tặng 1 tháng khi đăng ký 12 tháng">
                <span asp-validation-for="UuDaiDacBiet" class="text-red-500 text-sm"></span>
            </div>

            <!-- Preview Card -->
            <div class="mb-6 p-4 bg-gray-50 rounded-lg">
                <h4 class="font-semibold text-gray-900 mb-3">🔍 Xem trước gói tập</h4>
                <div class="bg-white rounded-lg shadow-sm p-4 max-w-sm">
                    <div class="bg-gradient-to-r from-blue-500 to-purple-600 p-4 text-white rounded-t-lg">
                        <h5 class="font-bold text-lg" id="previewName">@Model.TenGoi</h5>
                        <div class="text-center mt-2">
                            <div class="text-2xl font-bold" id="previewPrice">@Model.Gia.ToString("N0")</div>
                            <div class="text-sm opacity-90">VNĐ / <span id="previewDuration">@Model.ThoiHanThang</span> tháng</div>
                        </div>
                    </div>
                    <div class="p-4">
                        <div class="text-sm text-gray-600" id="previewSessions">
                            @if (Model.SoBuoiToiDa.HasValue)
                            {
                                <span>Số buổi: @Model.SoBuoiToiDa buổi</span>
                            }
                            else
                            {
                                <span>Không giới hạn buổi</span>
                            }
                        </div>
                        <div class="text-sm text-gray-600 mt-2" id="previewDescription">@(Model.MoTa ?? "Mô tả gói tập")</div>
                    </div>
                </div>
            </div>

            <!-- Warning for Active Package -->
            @if (Model.TrangThai == "ACTIVE")
            {
                <div class="mb-6 p-4 bg-yellow-50 border-l-4 border-yellow-400">
                    <div class="flex items-center">
                        <svg class="w-5 h-5 text-yellow-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                        </svg>
                        <div>
                            <h4 class="font-medium text-yellow-800">Lưu ý quan trọng</h4>
                            <p class="text-sm text-yellow-700 mt-1">
                                Gói tập này đang hoạt động và có thể đã có người đăng ký. 
                                Việc thay đổi giá hoặc thời hạn có thể ảnh hưởng đến các đăng ký hiện tại.
                            </p>
                        </div>
                    </div>
                </div>
            }

            <!-- Action Buttons -->
            <div class="flex flex-col sm:flex-row gap-4 mt-8 pt-6 border-t border-gray-200">
                <button type="submit" class="flex-1 bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded-xl font-semibold transition-all duration-200 shadow-lg hover:shadow-xl">
                    <div class="flex items-center justify-center space-x-2">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                        <span>Cập nhật gói tập</span>
                    </div>
                </button>
                <a href="@Url.Action("Details", new { id = Model.GoiTapId })" class="flex-1 bg-gray-100 hover:bg-gray-200 text-gray-700 px-8 py-4 rounded-xl font-semibold transition-all duration-200 text-center">
                    <div class="flex items-center justify-center space-x-2">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                        </svg>
                        <span>Xem chi tiết</span>
                    </div>
                </a>
                <a href="@Url.Action("Index")" class="flex-1 bg-gray-100 hover:bg-gray-200 text-gray-700 px-8 py-4 rounded-xl font-semibold transition-all duration-200 text-center">
                    <div class="flex items-center justify-center space-x-2">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                        <span>Hủy bỏ</span>
                    </div>
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    
    <script>
        // Live preview functionality
        document.addEventListener('DOMContentLoaded', function() {
            const nameInput = document.getElementById('TenGoi');
            const priceInput = document.getElementById('Gia');
            const durationInput = document.getElementById('ThoiHanThang');
            const sessionsInput = document.getElementById('SoBuoiToiDa');
            const descriptionInput = document.getElementById('MoTa');
            
            const previewName = document.getElementById('previewName');
            const previewPrice = document.getElementById('previewPrice');
            const previewDuration = document.getElementById('previewDuration');
            const previewSessions = document.getElementById('previewSessions');
            const previewDescription = document.getElementById('previewDescription');
            
            function updatePreview() {
                previewName.textContent = nameInput.value || 'Tên gói tập';
                previewPrice.textContent = formatNumber(priceInput.value || 0);
                previewDuration.textContent = durationInput.value || 0;
                
                if (sessionsInput.value) {
                    previewSessions.textContent = `Số buổi: ${sessionsInput.value} buổi`;
                } else {
                    previewSessions.textContent = 'Không giới hạn buổi';
                }
                
                previewDescription.textContent = descriptionInput.value || 'Mô tả gói tập';
            }
            
            function formatNumber(num) {
                return new Intl.NumberFormat('vi-VN').format(num);
            }
            
            // Add event listeners
            nameInput.addEventListener('input', updatePreview);
            priceInput.addEventListener('input', updatePreview);
            durationInput.addEventListener('input', updatePreview);
            sessionsInput.addEventListener('input', updatePreview);
            descriptionInput.addEventListener('input', updatePreview);
        });
    </script>
}
