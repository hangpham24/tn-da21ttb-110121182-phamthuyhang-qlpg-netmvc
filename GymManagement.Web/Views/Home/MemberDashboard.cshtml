@model GymManagement.Web.Models.ViewModels.MemberDashboardViewModel

@{
    ViewData["Title"] = "Dashboard - Thành viên";
    Layout = "_MemberLayout";
}

<div class="min-h-screen bg-gray-50">
    <!-- Header Section -->
    <div class="bg-white border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center">
                <div>
                    <h1 class="text-3xl font-bold text-gray-900 mb-2">Chào mừng, @Model.MemberName! 👋</h1>
                    <p class="text-gray-600">Hôm nay là ngày tuyệt vời để tập luyện</p>
                </div>
                <div class="mt-4 sm:mt-0 flex space-x-3">
                    <a asp-controller="Booking" asp-action="Calendar" class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                        </svg>
                        Xem lịch
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <!-- Quick Stats Cards -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            <!-- Active Registrations -->
            <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <div class="w-8 h-8 bg-blue-500 rounded-lg flex items-center justify-center">
                            <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
                            </svg>
                        </div>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm font-medium text-gray-600">Đăng ký đang hoạt động</p>
                        <p class="text-2xl font-bold text-gray-900">@Model.QuickStats.ActiveRegistrations</p>
                    </div>
                </div>
            </div>

            <!-- Total Spent -->
            <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <div class="w-8 h-8 bg-green-500 rounded-lg flex items-center justify-center">
                            <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
                            </svg>
                        </div>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm font-medium text-gray-600">Tổng chi tiêu</p>
                        <p class="text-2xl font-bold text-gray-900">@Model.TotalSpent.ToString("N0") VNĐ</p>
                    </div>
                </div>
            </div>

            <!-- Upcoming Bookings -->
            <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <div class="w-8 h-8 bg-purple-500 rounded-lg flex items-center justify-center">
                            <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                            </svg>
                        </div>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm font-medium text-gray-600">Lịch sắp tới</p>
                        <p class="text-2xl font-bold text-gray-900">@Model.QuickStats.UpcomingBookings</p>
                    </div>
                </div>
            </div>

            <!-- Notifications -->
            <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <div class="w-8 h-8 bg-yellow-500 rounded-lg flex items-center justify-center">
                            <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-5 5v-5zM9 17V7a4 4 0 118 0v10"></path>
                            </svg>
                        </div>
                    </div>
                    <div class="ml-4">
                        <p class="text-sm font-medium text-gray-600">Thông báo mới</p>
                        <p class="text-2xl font-bold text-gray-900">@Model.QuickStats.UnreadNotifications</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Dashboard Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Left Column -->
            <div class="lg:col-span-2 space-y-8">
                <!-- Membership Status -->
                <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                    <h3 class="text-lg font-semibold text-gray-900 mb-4">📍 Trạng thái thành viên</h3>
                    <div class="flex items-center p-4 bg-green-50 border border-green-200 rounded-lg">
                        <div class="flex-shrink-0">
                            <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                        </div>
                        <div class="ml-3">
                            <p class="text-sm font-medium text-green-800">✅ Thành viên @Model.CurrentMembershipStatus</p>
                            <p class="text-sm text-green-600">Check-in/checkout tại quầy lễ tân</p>
                        </div>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.QuickStats.NextClassTime))
                    {
                        <div class="mt-4 p-4 bg-purple-50 border border-purple-200 rounded-lg">
                            <div class="flex items-center">
                                <div class="flex-shrink-0">
                                    <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                    </svg>
                                </div>
                                <div class="ml-3">
                                    <p class="text-sm font-medium text-purple-800">🏃 Lớp tiếp theo</p>
                                    <p class="text-sm text-purple-600">@Model.QuickStats.NextClassTime</p>
                                </div>
                            </div>
                        </div>
                    }
                </div>

                <!-- Upcoming Bookings -->
                <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-lg font-semibold text-gray-900">📅 Lịch sắp tới</h3>
                        <a asp-controller="Booking" asp-action="MyBookings" class="text-sm text-blue-600 hover:text-blue-800">Xem tất cả</a>
                    </div>
                    @if (Model.UpcomingBookings.Any())
                    {
                        <div class="space-y-3">
                            @foreach (var booking in Model.UpcomingBookings)
                            {
                                <div class="flex items-center p-3 bg-gray-50 rounded-lg">
                                    <div class="flex-shrink-0 w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                                        <span class="text-sm font-medium text-blue-600">@booking.Ngay.ToString("dd")</span>
                                    </div>
                                    <div class="ml-4 flex-1">
                                        <p class="text-sm font-medium text-gray-900">@(booking.LopHoc?.TenLop ?? "Lớp học")</p>
                                        <p class="text-sm text-gray-600">
                                            @booking.Ngay.ToString("dddd, dd/MM") - 
                                            @(booking.LichLop?.GioBatDau.ToString("HH:mm") ?? "")
                                        </p>
                                    </div>
                                    <div class="text-right">
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                            Đã đặt
                                        </span>
                                    </div>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-6">
                            <svg class="w-12 h-12 text-gray-400 mx-auto mb-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                            </svg>
                            <p class="text-gray-500 text-sm">Chưa có lịch nào sắp tới</p>
                            <a asp-controller="Booking" asp-action="Calendar" class="mt-2 inline-flex items-center text-sm text-blue-600 hover:text-blue-800">
                                Đặt lịch ngay
                            </a>
                        </div>
                    }
                </div>
            </div>

            <!-- Right Column -->
            <div class="space-y-8">
                <!-- Payment Status -->
                @if (Model.QuickStats.HasPendingPayments)
                {
                    <div class="bg-yellow-50 border border-yellow-200 rounded-xl p-6">
                        <div class="flex items-center mb-3">
                            <svg class="w-6 h-6 text-yellow-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                            </svg>
                            <h3 class="text-lg font-semibold text-yellow-800">💳 Thanh toán chờ xử lý</h3>
                        </div>
                        <p class="text-yellow-700 text-sm mb-4">Bạn có @Model.PaymentStats.PendingPayments khoản thanh toán đang chờ xử lý</p>
                        <a asp-controller="Member" asp-action="MyRegistrations" class="inline-flex items-center px-4 py-2 bg-yellow-600 text-white rounded-md text-sm font-medium hover:bg-yellow-700">
                            Thanh toán ngay
                        </a>
                    </div>
                }

                <!-- My Registrations -->
                <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-lg font-semibold text-gray-900">📋 Đăng ký của tôi</h3>
                        <a asp-controller="Member" asp-action="MyRegistrations" class="text-sm text-blue-600 hover:text-blue-800">Xem tất cả</a>
                    </div>
                    @if (Model.ActiveRegistrations.Any())
                    {
                        <div class="space-y-3">
                            @foreach (var registration in Model.ActiveRegistrations.Take(3))
                            {
                                <div class="flex items-center justify-between p-3 bg-green-50 rounded-lg">
                                    <div>
                                        <p class="text-sm font-medium text-gray-900">
                                            @(registration.GoiTap?.TenGoi ?? registration.LopHoc?.TenLop ?? "Đăng ký")
                                        </p>
                                        <p class="text-sm text-gray-600">
                                            Đến @registration.NgayKetThuc.ToString("dd/MM/yyyy")
                                        </p>
                                    </div>
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                        Hoạt động
                                    </span>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <p class="text-gray-500 text-sm">Chưa có đăng ký nào</p>
                            <a asp-controller="Member" asp-action="Packages" class="mt-2 inline-flex items-center text-sm text-blue-600 hover:text-blue-800">
                                Xem gói tập
                            </a>
                        </div>
                    }
                </div>

                <!-- Notifications -->
                <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-lg font-semibold text-gray-900">🔔 Thông báo</h3>
                        <span class="text-sm text-gray-500">@Model.UnreadNotifications.Count mới</span>
                    </div>
                    @if (Model.UnreadNotifications.Any())
                    {
                        <div class="space-y-3">
                            @foreach (var notification in Model.UnreadNotifications)
                            {
                                <div class="p-3 bg-blue-50 rounded-lg">
                                    <p class="text-sm font-medium text-gray-900">@notification.TieuDe</p>
                                    <p class="text-sm text-gray-600 mt-1">@notification.NoiDung</p>
                                    <p class="text-xs text-gray-500 mt-2">@notification.NgayTao.ToString("dd/MM HH:mm")</p>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <p class="text-gray-500 text-sm">Không có thông báo mới</p>
                        </div>
                    }
                </div>

                <!-- Recommended Classes -->
                <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-100">
                    <h3 class="text-lg font-semibold text-gray-900 mb-4">⭐ Lớp học đề xuất</h3>
                    @if (Model.RecommendedClasses.Any())
                    {
                        <div class="space-y-3">
                            @foreach (var lopHoc in Model.RecommendedClasses)
                            {
                                <div class="p-3 border border-gray-200 rounded-lg">
                                    <p class="text-sm font-medium text-gray-900">@lopHoc.TenLop</p>
                                    <p class="text-sm text-gray-600">@lopHoc.GioBatDau.ToString("HH:mm") - @lopHoc.GioKetThuc.ToString("HH:mm")</p>
                                    <p class="text-xs text-gray-500 mt-1">@lopHoc.ThuTrongTuan</p>
                                </div>
                            }
                        </div>
                        <a asp-controller="Member" asp-action="Classes" class="mt-4 block text-center text-sm text-blue-600 hover:text-blue-800">
                            Xem tất cả lớp học
                        </a>
                    }
                    else
                    {
                        <div class="text-center py-4">
                            <p class="text-gray-500 text-sm">Đang cập nhật...</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <!-- No attendance chart needed -->
}

