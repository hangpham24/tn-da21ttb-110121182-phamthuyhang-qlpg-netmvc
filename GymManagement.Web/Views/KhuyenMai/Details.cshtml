@model GymManagement.Web.Data.Models.KhuyenMai
@{
    ViewData["Title"] = "Chi tiết khuyến mãi";
    Layout = "_Layout";
}

<div class="p-6">
    <!-- Header -->
    <div class="mb-6">
        <div class="flex items-center justify-between mb-4">
            <div class="flex items-center">
                <a href="@Url.Action("Index")" class="text-gray-500 hover:text-gray-700 mr-4">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                    </svg>
                </a>
                <h1 class="text-2xl font-bold text-gray-900">🎫 Chi tiết khuyến mãi</h1>
            </div>
            <div class="flex space-x-3">
                <a href="@Url.Action("Edit", new { id = Model.KhuyenMaiId })" 
                   class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg flex items-center">
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                    </svg>
                    Chỉnh sửa
                </a>
                <a href="@Url.Action("Delete", new { id = Model.KhuyenMaiId })" 
                   class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg flex items-center">
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                    </svg>
                    Xóa
                </a>
            </div>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Main Info -->
        <div class="lg:col-span-2">
            <!-- Promotion Card -->
            <div class="bg-white rounded-lg shadow-lg overflow-hidden mb-6">
                <div class="bg-blue-600 px-6 py-8 text-white">
                    <div class="flex items-center justify-between">
                        <div>
                            <h2 class="text-3xl font-bold">@Model.MaCode</h2>
                            <p class="text-blue-100 mt-2">@(Model.MoTa ?? "Không có mô tả")</p>
                        </div>
                        <div class="text-right">
                            <div class="text-4xl font-bold">@(Model.PhanTramGiam ?? 0)%</div>
                            <div class="text-sm text-blue-100">GIẢM GIÁ</div>
                        </div>
                    </div>
                    <div class="mt-4 flex items-center text-sm text-blue-100">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3a1 1 0 011-1h6a1 1 0 011 1v4m-6 0h6m-6 0V7a1 1 0 00-1 1v11a2 2 0 002 2h8a2 2 0 002-2V8a1 1 0 00-1-1h-1V7"></path>
                        </svg>
                        Có hiệu lực từ @Model.NgayBatDau.ToString("dd/MM/yyyy") đến @Model.NgayKetThuc.ToString("dd/MM/yyyy")
                    </div>
                </div>
            </div>

            <!-- Details Table -->
            <div class="bg-white rounded-lg shadow">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h3 class="text-lg font-medium text-gray-900">Thông tin chi tiết</h3>
                </div>
                <div class="px-6 py-4">
                    <dl class="grid grid-cols-1 gap-x-4 gap-y-6 sm:grid-cols-2">
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Mã khuyến mãi</dt>
                            <dd class="mt-1 text-sm text-gray-900 font-mono bg-gray-100 px-2 py-1 rounded">@Model.MaCode</dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Phần trăm giảm giá</dt>
                            <dd class="mt-1 text-sm text-gray-900">
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                    @(Model.PhanTramGiam ?? 0)%
                                </span>
                            </dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Ngày bắt đầu</dt>
                            <dd class="mt-1 text-sm text-gray-900">@Model.NgayBatDau.ToString("dd/MM/yyyy")</dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Ngày kết thúc</dt>
                            <dd class="mt-1 text-sm text-gray-900">@Model.NgayKetThuc.ToString("dd/MM/yyyy")</dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Ngày tạo</dt>
                            <dd class="mt-1 text-sm text-gray-900">@Model.NgayTao.ToString("dd/MM/yyyy HH:mm")</dd>
                        </div>
                        <div>
                            <dt class="text-sm font-medium text-gray-500">Trạng thái kích hoạt</dt>
                            <dd class="mt-1 text-sm text-gray-900">
                                @if (Model.KichHoat)
                                {
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                        <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                                        </svg>
                                        Đã kích hoạt
                                    </span>
                                }
                                else
                                {
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                                        <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>
                                        </svg>
                                        Chưa kích hoạt
                                    </span>
                                }
                            </dd>
                        </div>
                        <div class="sm:col-span-2">
                            <dt class="text-sm font-medium text-gray-500">Mô tả</dt>
                            <dd class="mt-1 text-sm text-gray-900">
                                @if (!string.IsNullOrEmpty(Model.MoTa))
                                {
                                    <p class="bg-gray-50 p-3 rounded-md">@Model.MoTa</p>
                                }
                                else
                                {
                                    <p class="text-gray-500 italic">Không có mô tả</p>
                                }
                            </dd>
                        </div>
                    </dl>
                </div>
            </div>
        </div>

        <!-- Sidebar -->
        <div class="lg:col-span-1">
            <!-- Status Card -->
            <div class="bg-white rounded-lg shadow mb-6">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h3 class="text-lg font-medium text-gray-900">Trạng thái hiện tại</h3>
                </div>
                <div class="px-6 py-4">
                    @{
                        var today = DateOnly.FromDateTime(DateTime.Today);
                        var isActive = Model.KichHoat && Model.NgayBatDau <= today && Model.NgayKetThuc >= today;
                        var isExpired = Model.NgayKetThuc < today;
                        var isUpcoming = Model.NgayBatDau > today;
                    }
                    
                    <div class="text-center">
                        @if (isActive)
                        {
                            <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                            </div>
                            <h4 class="text-lg font-semibold text-green-600">Đang hoạt động</h4>
                            <p class="text-sm text-gray-600 mt-2">Khuyến mãi đang có hiệu lực và có thể sử dụng</p>
                        }
                        else if (isExpired)
                        {
                            <div class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                            </div>
                            <h4 class="text-lg font-semibold text-red-600">Đã hết hạn</h4>
                            <p class="text-sm text-gray-600 mt-2">Khuyến mãi đã hết hiệu lực</p>
                        }
                        else if (isUpcoming)
                        {
                            <div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                            </div>
                            <h4 class="text-lg font-semibold text-blue-600">Sắp diễn ra</h4>
                            <p class="text-sm text-gray-600 mt-2">Khuyến mãi chưa có hiệu lực</p>
                        }
                        else
                        {
                            <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
                                <svg class="w-8 h-8 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728L5.636 5.636m12.728 12.728L5.636 5.636"></path>
                                </svg>
                            </div>
                            <h4 class="text-lg font-semibold text-gray-600">Đã tắt</h4>
                            <p class="text-sm text-gray-600 mt-2">Khuyến mãi đã bị vô hiệu hóa</p>
                        }
                    </div>

                    <!-- Time remaining -->
                    @if (Model.NgayKetThuc >= today)
                    {
                        var daysLeft = (Model.NgayKetThuc.ToDateTime(TimeOnly.MinValue) - DateTime.Today).Days;
                        <div class="mt-6 pt-6 border-t border-gray-200">
                            <div class="text-center">
                                <div class="text-2xl font-bold text-gray-900">@daysLeft</div>
                                <div class="text-sm text-gray-500">ngày còn lại</div>
                            </div>
                        </div>
                    }
                </div>
            </div>

            <!-- Test Promotion -->
            <div class="bg-white rounded-lg shadow">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h3 class="text-lg font-medium text-gray-900">Kiểm tra khuyến mãi</h3>
                </div>
                <div class="px-6 py-4">
                    <div class="space-y-4">
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-2">Số tiền gốc (VNĐ)</label>
                            <input type="number" id="testAmount" class="w-full px-3 py-2 border border-gray-300 rounded-md" 
                                   placeholder="1000000" value="1000000">
                        </div>
                        <button onclick="testPromotion()" 
                                class="w-full bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md">
                            Kiểm tra
                        </button>
                        <div id="testResult" class="hidden">
                            <div class="bg-gray-50 p-3 rounded-md">
                                <div class="text-sm">
                                    <div class="flex justify-between">
                                        <span>Số tiền gốc:</span>
                                        <span id="originalAmount"></span>
                                    </div>
                                    <div class="flex justify-between text-green-600">
                                        <span>Giảm giá:</span>
                                        <span id="discountAmount"></span>
                                    </div>
                                    <div class="flex justify-between font-semibold border-t pt-2 mt-2">
                                        <span>Số tiền cuối:</span>
                                        <span id="finalAmount"></span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function testPromotion() {
            const amount = parseFloat($('#testAmount').val()) || 0;
            const discountPercent = @(Model.PhanTramGiam ?? 0);
            
            if (amount <= 0) {
                alert('Vui lòng nhập số tiền hợp lệ');
                return;
            }

            const discountAmount = amount * discountPercent / 100;
            const finalAmount = amount - discountAmount;

            $('#originalAmount').text(amount.toLocaleString('vi-VN') + ' VNĐ');
            $('#discountAmount').text('-' + discountAmount.toLocaleString('vi-VN') + ' VNĐ');
            $('#finalAmount').text(finalAmount.toLocaleString('vi-VN') + ' VNĐ');
            
            $('#testResult').removeClass('hidden');
        }
    </script>
}
