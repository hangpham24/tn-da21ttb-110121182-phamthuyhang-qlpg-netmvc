<!-- QR Payment Modal -->
<div id="qrPaymentModal" class="fixed inset-0 bg-gray-600 bg-opacity-50 hidden z-50">
    <div class="flex items-center justify-center min-h-screen p-4">
        <div class="bg-white rounded-lg shadow-xl max-w-md w-full">
            <div class="p-6">
                <!-- Modal Header -->
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-lg font-semibold text-gray-900">💳 Thanh toán VNPay</h3>
                    <button type="button" onclick="closeQRPaymentModal()" class="text-gray-400 hover:text-gray-600 transition-colors duration-200">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                    </button>
                </div>

                <!-- Payment Info -->
                <div class="text-center mb-6">
                    <div class="bg-blue-50 rounded-lg p-4 mb-4">
                        <p class="text-sm text-blue-800 mb-2">Số tiền thanh toán:</p>
                        <p class="text-2xl font-bold text-blue-900" id="qrPaymentAmount">0 VNĐ</p>
                        <p class="text-xs text-blue-600 mt-1" id="qrOrderId">Mã đơn hàng: -</p>
                    </div>
                </div>

                <!-- QR Code Display -->
                <div class="text-center mb-6">
                    <div class="bg-white border-2 border-gray-200 rounded-lg p-4 inline-block">
                        <div id="qrCodeContainer" class="flex items-center justify-center" style="min-height: 200px;">
                            <div class="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600"></div>
                        </div>
                    </div>
                    <p class="text-sm text-gray-600 mt-2">Quét mã QR bằng ứng dụng ngân hàng để thanh toán</p>

                    <!-- Bank Info -->
                    <div class="mt-3 text-xs text-gray-500" id="bankInfo" style="display: none;">
                        <p>Ngân hàng: <span id="bankName">-</span></p>
                        <p>Số tài khoản: <span id="accountNumber">-</span></p>
                        <p>Chủ tài khoản: <span id="accountName">-</span></p>
                    </div>
                </div>

                <!-- Payment Options -->
                <div class="space-y-3">
                    <!-- QR Code Option -->
                    <button type="button" id="showQRBtn" onclick="showQRCode()" 
                            class="w-full bg-blue-600 hover:bg-blue-700 text-white px-4 py-3 rounded-md font-medium transition-colors duration-200 flex items-center justify-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v1m6 11h2m-6 0h-2v4m0-11v3m0 0h.01M12 12h4.01M16 20h4M4 12h4m12 0h2M4 4h16a2 2 0 012 2v12a2 2 0 01-2 2H4a2 2 0 01-2-2V6a2 2 0 012-2z"></path>
                        </svg>
                        Hiển thị mã QR
                    </button>

                    <!-- Browser Payment Option -->
                    <button type="button" onclick="openPaymentInBrowser()" 
                            class="w-full bg-green-600 hover:bg-green-700 text-white px-4 py-3 rounded-md font-medium transition-colors duration-200 flex items-center justify-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                        </svg>
                        Mở trang thanh toán
                    </button>

                    <!-- Cancel Option -->
                    <button type="button" onclick="closeQRPaymentModal()" 
                            class="w-full bg-gray-300 hover:bg-gray-400 text-gray-700 px-4 py-3 rounded-md font-medium transition-colors duration-200">
                        Hủy
                    </button>
                </div>

                <!-- Instructions -->
                <div class="mt-4 p-3 bg-yellow-50 rounded-lg">
                    <div class="flex items-start">
                        <svg class="w-5 h-5 text-yellow-600 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <div class="text-sm text-yellow-800">
                            <p class="font-medium mb-1">Hướng dẫn thanh toán:</p>
                            <ul class="list-disc list-inside space-y-1 text-xs">
                                <li>Mở ứng dụng ngân hàng trên điện thoại</li>
                                <li>Chọn tính năng quét mã QR</li>
                                <li>Quét mã QR hiển thị trên màn hình</li>
                                <li>Xác nhận thông tin và hoàn tất thanh toán</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
let currentPaymentData = null;

function showQRPaymentModal(paymentData) {
    currentPaymentData = paymentData;

    // Update payment info
    document.getElementById('qrPaymentAmount').textContent =
        new Intl.NumberFormat('vi-VN').format(paymentData.amount) + ' VNĐ';
    document.getElementById('qrOrderId').textContent =
        'Mã đơn hàng: ' + paymentData.orderId;

    // Update bank info if available
    if (paymentData.bankInfo) {
        document.getElementById('bankName').textContent = getBankName(paymentData.bankInfo.bankId);
        document.getElementById('accountNumber').textContent = paymentData.bankInfo.accountNo;
        document.getElementById('accountName').textContent = paymentData.bankInfo.accountName;
        document.getElementById('bankInfo').style.display = 'block';
    }

    // Show modal
    document.getElementById('qrPaymentModal').classList.remove('hidden');

    // Auto-generate QR code
    setTimeout(() => {
        showQRCode();
    }, 500);
}

function closeQRPaymentModal() {
    document.getElementById('qrPaymentModal').classList.add('hidden');
    currentPaymentData = null;
    
    // Clear QR code
    document.getElementById('qrCodeContainer').innerHTML = 
        '<div class="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600"></div>';
}

function showQRCode() {
    if (!currentPaymentData) {
        alert('Không có dữ liệu thanh toán');
        return;
    }

    const container = document.getElementById('qrCodeContainer');
    container.innerHTML = '<div class="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600"></div>';

    // Try to use VietQR image first (better compatibility with banking apps)
    if (currentPaymentData.qrImageUrl) {
        const img = new Image();
        img.onload = function() {
            container.innerHTML = '';
            img.style.width = '200px';
            img.style.height = '200px';
            img.style.border = '1px solid #e5e7eb';
            img.style.borderRadius = '8px';
            container.appendChild(img);
        };
        img.onerror = function() {
            // Fallback to QRCode.js if VietQR image fails
            generateQRCodeWithJS();
        };
        img.src = currentPaymentData.qrImageUrl;
    } else {
        // Fallback to QRCode.js
        generateQRCodeWithJS();
    }
}

function generateQRCodeWithJS() {
    const container = document.getElementById('qrCodeContainer');

    if (!currentPaymentData.qrCodeData) {
        container.innerHTML = `
            <div class="text-center text-red-600">
                <svg class="w-12 h-12 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <p class="text-sm">Không có dữ liệu QR</p>
                <button onclick="openPaymentInBrowser()" class="mt-2 text-blue-600 hover:text-blue-800 text-sm underline">
                    Mở trang thanh toán
                </button>
            </div>
        `;
        return;
    }

    // Generate QR code using QRCode.js library
    try {
        // Clear container
        container.innerHTML = '';

        // Create QR code
        QRCode.toCanvas(container, currentPaymentData.qrCodeData, {
            width: 200,
            height: 200,
            margin: 2,
            color: {
                dark: '#000000',
                light: '#FFFFFF'
            }
        }, function (error) {
            if (error) {
                console.error('QR Code generation error:', error);
                container.innerHTML = `
                    <div class="text-center text-red-600">
                        <svg class="w-12 h-12 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <p class="text-sm">Không thể tạo mã QR</p>
                        <button onclick="openPaymentInBrowser()" class="mt-2 text-blue-600 hover:text-blue-800 text-sm underline">
                            Mở trang thanh toán
                        </button>
                    </div>
                `;
            }
        });
    } catch (error) {
        console.error('QR Code library error:', error);
        container.innerHTML = `
            <div class="text-center text-red-600">
                <svg class="w-12 h-12 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                <p class="text-sm">Thư viện QR Code chưa được tải</p>
                <button onclick="openPaymentInBrowser()" class="mt-2 text-blue-600 hover:text-blue-800 text-sm underline">
                    Mở trang thanh toán
                </button>
            </div>
        `;
    }
}

function openPaymentInBrowser() {
    if (currentPaymentData && currentPaymentData.paymentUrl) {
        window.open(currentPaymentData.paymentUrl, '_blank');
    } else {
        alert('Không có URL thanh toán');
    }
}

function getBankName(bankId) {
    const bankNames = {
        '970415': 'Vietinbank',
        '970436': 'Vietcombank',
        '970418': 'BIDV',
        '970405': 'Agribank',
        '970422': 'MB Bank',
        '970407': 'Techcombank',
        '970432': 'VPBank',
        '970423': 'TPBank',
        '970403': 'Sacombank',
        '970448': 'OCB'
    };
    return bankNames[bankId] || 'Ngân hàng';
}
</script>
