@model GymManagement.Web.Models.DTOs.CreateNguoiDungDto
@{
    ViewData["Title"] = "Thêm người dùng mới";
    Layout = "_Layout";
}

<div class="min-h-screen bg-gray-50">
    <!-- Header Section -->
    <div class="bg-white border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between py-6">
                <div class="flex items-center space-x-4">
                    <a href="@Url.Action("Index")" class="inline-flex items-center text-sm font-medium text-gray-500 hover:text-gray-700">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                        </svg>
                        Quay lại danh sách
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <div class="flex-shrink-0">
                        <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center">
                            <svg class="w-7 h-7 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
                            </svg>
                        </div>
                    </div>
                    <div>
                        <h1 class="text-2xl font-bold text-gray-900">Thêm người dùng mới</h1>
                        <p class="text-sm text-gray-600">Điền đầy đủ thông tin để tạo người dùng mới</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Content Section -->
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <form asp-action="Create" method="post" enctype="multipart/form-data" class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden" id="createUserForm">
            @Html.AntiForgeryToken()

            <!-- Form Header -->
            <div class="bg-gray-50 px-6 py-4 border-b border-gray-200">
                <h3 class="text-lg font-medium text-gray-900">Thông tin người dùng</h3>
                <p class="mt-1 text-sm text-gray-600">Điền đầy đủ thông tin để tạo người dùng mới</p>
            </div>

            <!-- Validation Summary -->
            <div asp-validation-summary="All" class="bg-red-50 border-l-4 border-red-400 p-4 m-6 mb-0" style="display: none;" id="validationSummary">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                        </svg>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-sm font-medium text-red-800">Có lỗi xảy ra khi tạo người dùng</h3>
                        <div class="mt-2 text-sm text-red-700" id="errorList">
                            <p>Vui lòng kiểm tra lại thông tin và thử lại.</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Form Content -->
            <div class="p-6">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- Left Column -->
                    <div class="space-y-6">
                        <!-- User Type -->
                        <div class="form-group">
                            <label asp-for="LoaiNguoiDung" class="block text-sm font-medium text-gray-700 mb-2">
                                <span class="flex items-center">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
                                    </svg>
                                    Loại người dùng <span class="text-red-500">*</span>
                                </span>
                            </label>
                            <select asp-for="LoaiNguoiDung" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200" required>
                                <option value="">-- Chọn loại người dùng --</option>
                                <option value="THANHVIEN">👤 Thành viên</option>
                                <option value="HLV">💪 Huấn luyện viên</option>
                                <option value="ADMIN">👔 Quản trị viên</option>
                                <option value="VANGLAI">🚶 Vãng lai</option>
                            </select>
                            <span asp-validation-for="LoaiNguoiDung" class="text-red-500 text-sm mt-1 block"></span>
                        </div>

                        <!-- Personal Information -->
                        <div class="space-y-4">
                            <h4 class="text-sm font-medium text-gray-900 flex items-center">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                </svg>
                                Thông tin cá nhân
                            </h4>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <!-- Last Name -->
                                <div class="form-group">
                                    <label asp-for="Ho" class="block text-sm font-medium text-gray-700 mb-2">
                                        Họ <span class="text-red-500">*</span>
                                    </label>
                                    <div class="relative">
                                        <input asp-for="Ho" type="text"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="Ví dụ: Nguyễn" required>
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="Ho" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- First Name -->
                                <div class="form-group">
                                    <label asp-for="Ten" class="block text-sm font-medium text-gray-700 mb-2">Tên</label>
                                    <div class="relative">
                                        <input asp-for="Ten" type="text"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="Ví dụ: Văn A">
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="Ten" class="text-red-500 text-sm mt-1 block"></span>
                                </div>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <!-- Gender -->
                                <div class="form-group">
                                    <label asp-for="GioiTinh" class="block text-sm font-medium text-gray-700 mb-2">Giới tính</label>
                                    <select asp-for="GioiTinh" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200">
                                        <option value="">-- Chọn giới tính --</option>
                                        <option value="Nam">Nam</option>
                                        <option value="Nữ">Nữ</option>
                                        <option value="Khác">Khác</option>
                                    </select>
                                    <span asp-validation-for="GioiTinh" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- Date of Birth -->
                                <div class="form-group">
                                    <label asp-for="NgaySinh" class="block text-sm font-medium text-gray-700 mb-2">Ngày sinh</label>
                                    <div class="relative">
                                        <input asp-for="NgaySinh" type="date"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200">
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="NgaySinh" class="text-red-500 text-sm mt-1 block"></span>
                                </div>
                            </div>
                        </div>

                        <!-- Contact Information -->
                        <div class="space-y-4">
                            <h4 class="text-sm font-medium text-gray-900 flex items-center">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                </svg>
                                Thông tin liên hệ
                            </h4>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <!-- Phone -->
                                <div class="form-group">
                                    <label asp-for="SoDienThoai" class="block text-sm font-medium text-gray-700 mb-2">Số điện thoại</label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                                            </svg>
                                        </div>
                                        <input asp-for="SoDienThoai" type="tel"
                                               class="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="0123456789">
                                    </div>
                                    <span asp-validation-for="SoDienThoai" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- Email -->
                                <div class="form-group">
                                    <label asp-for="Email" class="block text-sm font-medium text-gray-700 mb-2">Email</label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                            </svg>
                                        </div>
                                        <input asp-for="Email" type="email"
                                               class="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="example@email.com">
                                    </div>
                                    <span asp-validation-for="Email" class="text-red-500 text-sm mt-1 block"></span>
                                </div>
                            </div>

                            <!-- Address -->
                            <div class="form-group">
                                <label asp-for="DiaChi" class="block text-sm font-medium text-gray-700 mb-2">Địa chỉ</label>
                                <div class="relative">
                                    <input asp-for="DiaChi" type="text"
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                           placeholder="Số nhà, đường, phường/xã, quận/huyện, tỉnh/thành phố">
                                    <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                        <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                        </svg>
                                    </div>
                                </div>
                                <span asp-validation-for="DiaChi" class="text-red-500 text-sm mt-1 block"></span>
                            </div>
                        </div>
                    </div>

                    <!-- Right Column -->
                    <div class="space-y-6">
                        <!-- Account Information -->
                        <div class="space-y-4">
                            <h4 class="text-sm font-medium text-gray-900 flex items-center">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                                </svg>
                                Thông tin tài khoản
                            </h4>
                            
                            <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
                                <div class="flex">
                                    <div class="flex-shrink-0">
                                        <svg class="h-5 w-5 text-yellow-400" viewBox="0 0 20 20" fill="currentColor">
                                            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
                                        </svg>
                                    </div>
                                    <div class="ml-3">
                                        <p class="text-sm text-yellow-700">
                                            Nếu bạn muốn người dùng có thể đăng nhập vào hệ thống, hãy điền thông tin tài khoản bên dưới.
                                        </p>
                                    </div>
                                </div>
                            </div>

                            <div class="space-y-4">
                                <!-- Username -->
                                <div class="form-group">
                                    <label asp-for="Username" class="block text-sm font-medium text-gray-700 mb-2">Tên đăng nhập</label>
                                    <div class="relative">
                                        <input asp-for="Username" type="text"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="username123">
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="Username" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- Role -->
                                <div class="form-group">
                                    <label asp-for="VaiTro" class="block text-sm font-medium text-gray-700 mb-2">Vai trò</label>
                                    <select asp-for="VaiTro" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200">
                                        <option value="">-- Chọn vai trò --</option>
                                        <option value="Member">Member (Thành viên)</option>
                                        <option value="Trainer">Trainer (Huấn luyện viên)</option>
                                        <option value="Admin">Admin (Quản trị viên)</option>
                                    </select>
                                    <span asp-validation-for="VaiTro" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- Password -->
                                <div class="form-group">
                                    <label asp-for="Password" class="block text-sm font-medium text-gray-700 mb-2">
                                        Mật khẩu <span class="text-red-500" id="passwordRequired" style="display: none;">*</span>
                                    </label>
                                    <div class="relative">
                                        <input asp-for="Password" type="password"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="••••••••">
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="Password" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- Confirm Password -->
                                <div class="form-group">
                                    <label asp-for="ConfirmPassword" class="block text-sm font-medium text-gray-700 mb-2">
                                        Xác nhận mật khẩu <span class="text-red-500" id="confirmPasswordRequired" style="display: none;">*</span>
                                    </label>
                                    <div class="relative">
                                        <input asp-for="ConfirmPassword" type="password"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="••••••••">
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="ConfirmPassword" class="text-red-500 text-sm mt-1 block"></span>
                                </div>
                            </div>
                        </div>

                        <!-- Avatar Upload -->
                        <div class="space-y-4">
                            <h4 class="text-sm font-medium text-gray-900 flex items-center">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                </svg>
                                Ảnh đại diện
                            </h4>
                            
                            <div class="flex items-center space-x-6">
                                <div class="flex-shrink-0">
                                    <div id="avatarPreview" class="h-24 w-24 rounded-lg bg-gray-100 flex items-center justify-center overflow-hidden">
                                        <svg class="h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                        </svg>
                                    </div>
                                </div>
                                <div>
                                    <label for="avatarFile" class="cursor-pointer bg-white py-2 px-3 border border-gray-300 rounded-lg shadow-sm text-sm leading-4 font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200">
                                        <span>Chọn ảnh</span>
                                        <input id="avatarFile" name="avatarFile" type="file" class="sr-only" accept="image/*">
                                    </label>
                                    <p class="text-xs text-gray-500 mt-2">PNG, JPG, GIF tối đa 5MB</p>
                                </div>
                            </div>
                        </div>

                        <!-- Status -->
                        <div class="form-group">
                            <label class="block text-sm font-medium text-gray-700 mb-2">
                                <span class="flex items-center">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                    </svg>
                                    Trạng thái
                                </span>
                            </label>
                            <div class="flex items-center p-3 bg-gray-50 rounded-lg">
                                <input asp-for="TrangThai" type="checkbox" class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" checked>
                                <label for="TrangThai" class="ml-2 block text-sm text-gray-900">
                                    Kích hoạt tài khoản ngay sau khi tạo
                                </label>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="flex gap-4 mt-8 pt-6 border-t border-gray-200">
                    <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-md font-medium transition-colors duration-200 flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                        </svg>
                        Tạo người dùng
                    </button>
                    <a href="@Url.Action("Index")" class="bg-gray-300 hover:bg-gray-400 text-gray-700 px-6 py-2 rounded-md font-medium transition-colors duration-200 flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                        Hủy bỏ
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Avatar preview
            const avatarInput = document.getElementById('avatarFile');
            const avatarPreview = document.getElementById('avatarPreview');
            
            avatarInput.addEventListener('change', function(e) {
                const file = e.target.files[0];
                if (file && file.type.startsWith('image/')) {
                    const reader = new FileReader();
                    reader.onload = function(e) {
                        avatarPreview.innerHTML = `<img src="${e.target.result}" class="h-24 w-24 rounded-lg object-cover" alt="Preview">`;
                    };
                    reader.readAsDataURL(file);
                }
            });

            // Show/hide password required based on username
            const usernameInput = document.querySelector('input[name="Username"]');
            const passwordInput = document.querySelector('input[name="Password"]');
            const confirmPasswordInput = document.querySelector('input[name="ConfirmPassword"]');
            const passwordRequired = document.getElementById('passwordRequired');
            const confirmPasswordRequired = document.getElementById('confirmPasswordRequired');

            function togglePasswordRequired() {
                if (usernameInput.value.trim()) {
                    passwordRequired.style.display = 'inline';
                    confirmPasswordRequired.style.display = 'inline';
                    passwordInput.setAttribute('required', 'required');
                    confirmPasswordInput.setAttribute('required', 'required');
                } else {
                    passwordRequired.style.display = 'none';
                    confirmPasswordRequired.style.display = 'none';
                    passwordInput.removeAttribute('required');
                    confirmPasswordInput.removeAttribute('required');
                }
            }

            usernameInput.addEventListener('input', togglePasswordRequired);
            togglePasswordRequired();

            // Form validation
            const form = document.getElementById('createUserForm');
            form.addEventListener('submit', function(e) {
                let isValid = true;
                const errors = [];

                // Clear previous errors
                document.querySelectorAll('.field-error').forEach(el => el.remove());

                // Validate required fields
                const nameInput = document.getElementById('Ho');
                if (!nameInput.value.trim()) {
                    errors.push('Họ không được để trống');
                    showFieldError(nameInput, 'Vui lòng nhập họ');
                    isValid = false;
                }

                // Check if username is provided but password is not
                if (usernameInput.value.trim() && !passwordInput.value.trim()) {
                    e.preventDefault();
                    showNotification('error', 'Vui lòng nhập mật khẩu khi tạo tài khoản đăng nhập.');
                    passwordInput.focus();
                    return false;
                }

                // Check password confirmation
                if (passwordInput.value && passwordInput.value !== confirmPasswordInput.value) {
                    e.preventDefault();
                    showNotification('error', 'Mật khẩu xác nhận không khớp.');
                    confirmPasswordInput.focus();
                    return false;
                }

                if (!isValid) {
                    e.preventDefault();
                    // Show validation summary
                    const validationSummary = document.getElementById('validationSummary');
                    const errorList = document.getElementById('errorList');
                    errorList.innerHTML = '<ul class="list-disc list-inside">' + 
                        errors.map(err => `<li>${err}</li>`).join('') + '</ul>';
                    validationSummary.style.display = 'block';
                    
                    // Scroll to top
                    window.scrollTo({ top: 0, behavior: 'smooth' });
                }
            });

            function showFieldError(field, message) {
                const error = document.createElement('span');
                error.className = 'field-error text-red-500 text-sm mt-1 block';
                error.textContent = message;
                field.parentElement.appendChild(error);
            }

            // Auto-suggest username based on name
            const hoInput = document.querySelector('input[name="Ho"]');
            const tenInput = document.querySelector('input[name="Ten"]');
            
            function suggestUsername() {
                if (!usernameInput.value && hoInput.value && tenInput.value) {
                    const ho = hoInput.value.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '');
                    const ten = tenInput.value.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '');
                    const suggestion = ten + ho.charAt(0) + Math.floor(Math.random() * 100);
                    usernameInput.placeholder = `Gợi ý: ${suggestion}`;
                }
            }

            hoInput.addEventListener('blur', suggestUsername);
            tenInput.addEventListener('blur', suggestUsername);

            // Show validation errors from server
            const serverErrors = document.querySelectorAll('.field-validation-error');
            const validationSummary = document.getElementById('validationSummary');
            
            // Check if there are any validation errors from ASP.NET Core
            const validationList = validationSummary.querySelector('.validation-summary-errors ul');
            
            if (serverErrors.length > 0 || (validationList && validationList.children.length > 0)) {
                validationSummary.style.display = 'block';
                // Scroll to top to show errors
                window.scrollTo({ top: 0, behavior: 'smooth' });
            }
        });

        // Show notification function
        function showNotification(type, message) {
            // Create notification element
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 p-4 rounded-lg shadow-lg transform transition-all duration-300 translate-x-full z-50`;
            
            if (type === 'success') {
                notification.classList.add('bg-green-50', 'border', 'border-green-200');
                notification.innerHTML = `
                    <div class="flex items-center">
                        <svg class="h-5 w-5 text-green-400 mr-3" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                        </svg>
                        <p class="text-sm font-medium text-green-800">${message}</p>
                    </div>
                `;
            } else {
                notification.classList.add('bg-red-50', 'border', 'border-red-200');
                notification.innerHTML = `
                    <div class="flex items-center">
                        <svg class="h-5 w-5 text-red-400 mr-3" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                        </svg>
                        <p class="text-sm font-medium text-red-800">${message}</p>
                    </div>
                `;
            }

            // Add to page
            document.body.appendChild(notification);

            // Animate in
            setTimeout(() => {
                notification.classList.remove('translate-x-full');
                notification.classList.add('translate-x-0');
            }, 100);

            // Remove after 5 seconds
            setTimeout(() => {
                notification.classList.add('translate-x-full');
                notification.classList.remove('translate-x-0');
                setTimeout(() => {
                    notification.remove();
                }, 300);
            }, 5000);
        }
    </script>
}
