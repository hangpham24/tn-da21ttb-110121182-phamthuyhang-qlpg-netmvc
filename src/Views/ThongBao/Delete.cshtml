@model GymManagement.Web.Data.Models.ThongBao
@{
    ViewData["Title"] = "Xóa thông báo";
    Layout = "_Layout";
}

div class="min-h-screen bg-gray-50"
    !-- Header Section --
    div class="bg-white border-b border-gray-200"
        div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"
            div class="flex items-center justify-between py-6"
                div class="flex items-center space-x-4"
                    a href="@Url.Action("Index")" class="inline-flex items-center text-sm font-medium text-gray-500 hover:text-gray-700"
                        svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                            path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/path
                        /svg
                        Quay lại danh sách
                    /a
                /div
                div class="flex items-center space-x-4"
                    h1 class="text-2xl font-bold text-gray-900"Xóa thông báo/h1
                /div
            /div
        /div
    /div

    !-- Content Section --
    div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8"
        h3 class="text-lg font-medium text-gray-900 mb-4"Bạn có chắc chắn muốn xóa thông báo này?/h3

        div class="bg-white shadow overflow-hidden sm:rounded-lg"
            div class="border-t border-gray-200"
                dc class="px-4 py-5 sm:px-6 bg-gray-50"
                    dt class="text-sm font-medium text-gray-500"Tiêu đề/dt
                    dd class="mt-1 text-sm text-gray-900"@Model.TieuDe/dd
                /dl
            /div
            div class="border-t border-gray-200"
                dc class="px-4 py-5 sm:px-6"
                    dt class="text-sm font-medium text-gray-500"Nội dung/dt
                    dd class="mt-1 text-sm text-gray-900"@Model.NoiDung/dd
                /dl
            /div
            div class="border-t border-gray-200"
                dc class="px-4 py-5 sm:px-6 bg-gray-50"
                    dt class="text-sm font-medium text-gray-500"Người dùng/dt
                    dd class="mt-1 text-sm text-gray-900"@Model.NguoiDung?.Ho @Model.NguoiDung?.Ten/dd
                /dl
            /div
            div class="border-t border-gray-200"
                dc class="px-4 py-5 sm:px-6"
                    dt class="text-sm font-medium text-gray-500"Kênh/dt
                    dd class="mt-1 text-sm text-gray-900"@Model.Kenh/dd
                /dl
            /div
            div class="border-t border-gray-200"
                dc class="px-4 py-5 sm:px-6 bg-gray-50"
                    dt class="text-sm font-medium text-gray-500"Ngày tạo/dt
                    dd class="mt-1 text-sm text-gray-900"@Model.NgayTao.ToString("dd/MM/yyyy")/dd
                /dl
            /div
        /div

        form asp-action="DeleteConfirmed" method="post" class="mt-6"
            @Html.AntiForgeryToken()
            input type="hidden" asp-for="ThongBaoId"

            div class="flex gap-4"
                button type="submit" class="bg-red-600 hover:bg-red-700 text-white px-6 py-2 rounded-md font-medium transition-colors duration-200"
                    svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                        path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/path
                    /svg
                    Xác nhận xóa
                /button
                a href="@Url.Action("Index")" class="bg-gray-300 hover:bg-gray-400 text-gray-700 px-6 py-2 rounded-md font-medium transition-colors duration-200"
                    svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                        path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/path
                    /svg
                    Hủy bỏ
                /a
            /div
        /form
    /div
/div
    <h4>@Model.TieuDe</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Nội Dung
        </dt>
        <dd class="col-sm-10">
            @Model.NoiDung
        </dd>
        <dt class="col-sm-2">
            Người Dùng
        </dt>
        <dd class="col-sm-10">
            @Model.NguoiDung?.Ho @Model.NguoiDung?.Ten
        </dd>
        <dt class="col-sm-2">
            Kênh
        </dt>
        <dd class="col-sm-10">
            @Model.Kenh
        </dd>
        <dt class="col-sm-2">
            Ngày Tạo
        </dt>
        <dd class="col-sm-10">
            @Model.NgayTao.ToString("dd/MM/yyyy")
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="ThongBaoId" />
        <input type="submit" value="Xóa" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Quay lại danh sách</a>
    </form>
</div>
