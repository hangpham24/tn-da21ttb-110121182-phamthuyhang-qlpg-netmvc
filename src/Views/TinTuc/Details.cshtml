@model GymManagement.Web.Models.DTOs.TinTucDetailDto
@{
    ViewData["Title"] = "Chi tiết tin tức";
    Layout = ViewData["Layout"]?.ToString() ?? "_Layout";
}

<div class="min-h-screen bg-gray-50">
    <!-- Header Section -->
    <div class="bg-white border-b border-gray-200 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between py-6">
                <div class="flex items-center space-x-4">
                    <a href="@Url.Action("Index")" class="text-gray-400 hover:text-gray-600 transition-colors duration-200">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                        </svg>
                    </a>
                    <div>
                        <h1 class="text-2xl font-bold text-gray-900">Chi tiết tin tức</h1>
                        <p class="text-sm text-gray-600">Xem thông tin chi tiết của tin tức</p>
                    </div>
                </div>
                <div class="flex items-center space-x-3">
                    @if (User.IsInRole("Admin"))
                    {
                        <a href="@Url.Action("Edit", new { id = Model.TinTucId })" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                            </svg>
                            Chỉnh sửa
                        </a>
                        <a href="@Url.Action("Delete", new { id = Model.TinTucId })" class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                            </svg>
                            Xóa
                        </a>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Content Section -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="bg-white shadow-lg rounded-lg overflow-hidden">
            <!-- News Header -->
            <div class="p-6 border-b border-gray-200">
                <div class="flex items-start justify-between">
                    <div class="flex-1">
                        <h2 class="text-3xl font-bold text-gray-900 mb-2">@Model.TieuDe</h2>
                        <p class="text-lg text-gray-600 mb-4">@Model.MoTaNgan</p>
                        
                        <div class="flex items-center space-x-4 text-sm text-gray-500">
                            <div class="flex items-center">
                                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                </svg>
                                @Model.TenTacGia
                            </div>
                            <div class="flex items-center">
                                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                </svg>
                                @Model.NgayTao.ToString("dd/MM/yyyy HH:mm")
                            </div>
                            <div class="flex items-center">
                                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                                </svg>
                                @Model.LuotXem lượt xem
                            </div>
                        </div>
                    </div>
                    
                    <!-- Status and Featured -->
                    <div class="flex flex-col items-end space-y-2 ml-4">
                        @{
                            var (statusClass, statusText, statusIcon) = Model.TrangThai switch
                            {
                                "DRAFT" => ("bg-gray-100 text-gray-800", "Nháp", "📝"),
                                "PUBLISHED" => ("bg-green-100 text-green-800", "Đã xuất bản", "✅"),
                                "ARCHIVED" => ("bg-yellow-100 text-yellow-800", "Lưu trữ", "📁"),
                                _ => ("bg-gray-100 text-gray-800", Model.TrangThai, "❓")
                            };
                        }
                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium @statusClass">
                            @statusIcon @statusText
                        </span>
                        
                        @if (Model.NoiBat)
                        {
                            <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-yellow-100 text-yellow-800">
                                ⭐ Nổi bật
                            </span>
                        }
                    </div>
                </div>
            </div>

            <!-- Featured Image -->
            @if (!string.IsNullOrEmpty(Model.AnhDaiDien))
            {
                <div class="p-6 bg-gray-50">
                    <img src="@Model.AnhDaiDien" alt="@Model.TieuDe" class="w-full max-w-4xl mx-auto rounded-lg shadow-md" />
                </div>
            }

            <!-- News Content -->
            <div class="p-6">
                <div class="prose prose-lg max-w-none">
                    @Html.Raw(Model.NoiDung)
                </div>
            </div>

            <!-- Metadata -->
            <div class="bg-gray-50 p-6 border-t border-gray-200">
                <h3 class="text-lg font-semibold text-gray-900 mb-4">Thông tin bổ sung</h3>
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <p class="text-sm font-medium text-gray-500">Ngày xuất bản</p>
                        <p class="mt-1 text-sm text-gray-900">
                            @if (Model.NgayXuatBan.HasValue)
                            {
                                @Model.NgayXuatBan.Value.ToString("dd/MM/yyyy HH:mm")
                            }
                            else
                            {
                                <span class="text-gray-400">Chưa xuất bản</span>
                            }
                        </p>
                    </div>
                    
                    <div>
                        <p class="text-sm font-medium text-gray-500">Cập nhật lần cuối</p>
                        <p class="mt-1 text-sm text-gray-900">
                            @if (Model.NgayCapNhat.HasValue)
                            {
                                @Model.NgayCapNhat.Value.ToString("dd/MM/yyyy HH:mm")
                            }
                            else
                            {
                                <span class="text-gray-400">Chưa cập nhật</span>
                            }
                        </p>
                    </div>
                    
                    <div>
                        <p class="text-sm font-medium text-gray-500">Slug/URL</p>
                        <p class="mt-1 text-sm text-gray-900 font-mono">@Model.Slug</p>
                    </div>
                    
                    <div>
                        <p class="text-sm font-medium text-gray-500">ID tin tức</p>
                        <p class="mt-1 text-sm text-gray-900">#@Model.TinTucId</p>
                    </div>
                </div>

                <!-- SEO Information -->
                <div class="mt-6 pt-6 border-t border-gray-200">
                    <h4 class="text-md font-semibold text-gray-900 mb-3">SEO & Meta Tags</h4>
                    
                    <div class="space-y-3">
                        <div>
                            <p class="text-sm font-medium text-gray-500">Meta Title</p>
                            <p class="mt-1 text-sm text-gray-900">
                                @(!string.IsNullOrEmpty(Model.MetaTitle) ? Model.MetaTitle : Model.TieuDe)
                            </p>
                        </div>
                        
                        <div>
                            <p class="text-sm font-medium text-gray-500">Meta Description</p>
                            <p class="mt-1 text-sm text-gray-900">
                                @(!string.IsNullOrEmpty(Model.MetaDescription) ? Model.MetaDescription : Model.MoTaNgan)
                            </p>
                        </div>
                        
                        @if (!string.IsNullOrEmpty(Model.MetaKeywords))
                        {
                            <div>
                                <p class="text-sm font-medium text-gray-500">Meta Keywords</p>
                                <p class="mt-1 text-sm text-gray-900">@Model.MetaKeywords</p>
                            </div>
                        }
                    </div>
                </div>
            </div>

            <!-- Actions -->
            <div class="bg-gray-50 px-6 py-4 border-t border-gray-200">
                <div class="flex justify-between items-center">
                    @if (User.IsInRole("Admin"))
                    {
                        <a href="@Url.Action("Index")" class="text-blue-600 hover:text-blue-800 text-sm font-medium">
                            ← Quay lại danh sách
                        </a>
                    }
                    else
                    {
                        <a href="@Url.Action("Index", "Home")" class="text-blue-600 hover:text-blue-800 text-sm font-medium">
                            ← Quay lại trang chủ
                        </a>
                    }
                    
                    @if (Model.TrangThai == "PUBLISHED" && !string.IsNullOrEmpty(Model.Slug))
                    {
                        <a href="@Url.Action("ChiTietTinTuc", "Home", new { slug = Model.Slug })" target="_blank" 
                           class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                            </svg>
                            Xem trên trang công khai
                        </a>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
