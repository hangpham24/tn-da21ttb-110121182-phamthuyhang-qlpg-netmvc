@model GymManagement.Web.Models.DTOs.UpdateNguoiDungDto
@{
    ViewData["Title"] = "Chỉnh sửa người dùng";
    Layout = "_Layout";
    var currentAvatar = ViewBag.CurrentAvatar as string;
}

<div class="min-h-screen bg-gray-50">
    <!-- Header Section -->
    <div class="bg-white border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between py-6">
                <div class="flex items-center space-x-4">
                    <a href="@Url.Action("Index")" class="inline-flex items-center text-sm font-medium text-gray-500 hover:text-gray-700">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                        </svg>
                        Quay lại danh sách
                    </a>
                </div>
                <div class="flex items-center space-x-4">
                    <div class="flex-shrink-0">
                        <div class="w-12 h-12 bg-orange-100 rounded-lg flex items-center justify-center">
                            <svg class="w-7 h-7 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                            </svg>
                        </div>
                    </div>
                    <div>
                        <h1 class="text-2xl font-bold text-gray-900">Chỉnh sửa người dùng</h1>
                        <p class="text-sm text-gray-600">Cập nhật thông tin người dùng trong hệ thống</p>
                    </div>
                </div>
                <div class="flex space-x-3">
                    <a href="@Url.Action("Details", new { id = Model.NguoiDungId })" class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                        <svg class="mr-2 -ml-1 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                        </svg>
                        Xem chi tiết
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Content Section -->
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <form asp-action="Edit" method="post" enctype="multipart/form-data" class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden" id="editUserForm">
            @Html.AntiForgeryToken()
            <input asp-for="NguoiDungId" type="hidden" />
            <input asp-for="NgayThamGia" type="hidden" />
            <input asp-for="NgayTao" type="hidden" />

            <!-- Form Header -->
            <div class="bg-gray-50 px-6 py-4 border-b border-gray-200">
                <h3 class="text-lg font-medium text-gray-900">Chỉnh sửa người dùng</h3>
                <p class="mt-1 text-sm text-gray-600">Cập nhật thông tin người dùng: <span class="font-medium text-gray-900">@Model.Ho @Model.Ten</span></p>
            </div>

            <!-- Validation Summary -->
            <div asp-validation-summary="ModelOnly" class="bg-red-50 border-l-4 border-red-400 p-4 m-6 mb-0" style="display: none;" id="validationSummary">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                        </svg>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-sm font-medium text-red-800">Có lỗi xảy ra khi cập nhật người dùng</h3>
                        <div class="mt-2 text-sm text-red-700" id="errorList">
                            <p>Vui lòng kiểm tra lại thông tin và thử lại.</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Form Content -->
            <div class="p-6">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- Left Column -->
                    <div class="space-y-6">
                        <!-- User Type -->
                        <div class="form-group">
                            <label asp-for="LoaiNguoiDung" class="block text-sm font-medium text-gray-700 mb-2">
                                <span class="flex items-center">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"></path>
                                    </svg>
                                    Loại người dùng <span class="text-red-500">*</span>
                                </span>
                            </label>
                            <select asp-for="LoaiNguoiDung" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200" required>
                                <option value="">-- Chọn loại người dùng --</option>
                                <option value="THANHVIEN">👤 Thành viên</option>
                                <option value="HLV">💪 Huấn luyện viên</option>
                                <option value="ADMIN">👔 Quản trị viên</option>
                                <option value="VANGLAI">🚶 Vãng lai</option>
                            </select>
                            <span asp-validation-for="LoaiNguoiDung" class="text-red-500 text-sm mt-1 block"></span>
                        </div>

                        <!-- Personal Information -->
                        <div class="space-y-4">
                            <h4 class="text-sm font-medium text-gray-900 flex items-center">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                </svg>
                                Thông tin cá nhân
                            </h4>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <!-- Last Name -->
                                <div class="form-group">
                                    <label asp-for="Ho" class="block text-sm font-medium text-gray-700 mb-2">
                                        Họ <span class="text-red-500">*</span>
                                    </label>
                                    <div class="relative">
                                        <input asp-for="Ho" type="text"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               required>
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="Ho" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- First Name -->
                                <div class="form-group">
                                    <label asp-for="Ten" class="block text-sm font-medium text-gray-700 mb-2">Tên</label>
                                    <div class="relative">
                                        <input asp-for="Ten" type="text"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200">
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="Ten" class="text-red-500 text-sm mt-1 block"></span>
                                </div>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <!-- Gender -->
                                <div class="form-group">
                                    <label asp-for="GioiTinh" class="block text-sm font-medium text-gray-700 mb-2">Giới tính</label>
                                    <select asp-for="GioiTinh" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200">
                                        <option value="">-- Chọn giới tính --</option>
                                        <option value="Nam">Nam</option>
                                        <option value="Nữ">Nữ</option>
                                        <option value="Khác">Khác</option>
                                    </select>
                                    <span asp-validation-for="GioiTinh" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- Date of Birth -->
                                <div class="form-group">
                                    <label asp-for="NgaySinh" class="block text-sm font-medium text-gray-700 mb-2">Ngày sinh</label>
                                    <div class="relative">
                                        <input asp-for="NgaySinh" type="date"
                                               class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200">
                                        <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                            </svg>
                                        </div>
                                    </div>
                                    <span asp-validation-for="NgaySinh" class="text-red-500 text-sm mt-1 block"></span>
                                </div>
                            </div>
                        </div>

                        <!-- Contact Information -->
                        <div class="space-y-4">
                            <h4 class="text-sm font-medium text-gray-900 flex items-center">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                </svg>
                                Thông tin liên hệ
                            </h4>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <!-- Phone -->
                                <div class="form-group">
                                    <label asp-for="SoDienThoai" class="block text-sm font-medium text-gray-700 mb-2">Số điện thoại</label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                                            </svg>
                                        </div>
                                        <input asp-for="SoDienThoai" type="tel"
                                               class="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="0123456789">
                                    </div>
                                    <span asp-validation-for="SoDienThoai" class="text-red-500 text-sm mt-1 block"></span>
                                </div>

                                <!-- Email -->
                                <div class="form-group">
                                    <label asp-for="Email" class="block text-sm font-medium text-gray-700 mb-2">Email</label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                            </svg>
                                        </div>
                                        <input asp-for="Email" type="email"
                                               class="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                               placeholder="example@email.com">
                                    </div>
                                    <span asp-validation-for="Email" class="text-red-500 text-sm mt-1 block"></span>
                                </div>
                            </div>

                            <!-- Address -->
                            <div class="form-group">
                                <label asp-for="DiaChi" class="block text-sm font-medium text-gray-700 mb-2">Địa chỉ</label>
                                <div class="relative">
                                    <input asp-for="DiaChi" type="text"
                                           class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200"
                                           placeholder="Số nhà, đường, phường/xã, quận/huyện, tỉnh/thành phố">
                                    <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
                                        <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                        </svg>
                                    </div>
                                </div>
                                <span asp-validation-for="DiaChi" class="text-red-500 text-sm mt-1 block"></span>
                            </div>
                        </div>
                    </div>

                    <!-- Right Column -->
                    <div class="space-y-6">
                        <!-- Avatar Upload -->
                        <div class="space-y-4">
                            <h4 class="text-sm font-medium text-gray-900 flex items-center">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                </svg>
                                Ảnh đại diện
                            </h4>
                            
                            <div class="flex items-center space-x-6">
                                <div class="flex-shrink-0">
                                    <div id="avatarPreview" class="h-24 w-24 rounded-lg bg-gray-100 flex items-center justify-center overflow-hidden">
                                        @if (!string.IsNullOrEmpty(currentAvatar))
                                        {
                                            <img src="@currentAvatar" class="h-24 w-24 rounded-lg object-cover" alt="Current avatar">
                                        }
                                        else
                                        {
                                            <svg class="h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                                            </svg>
                                        }
                                    </div>
                                </div>
                                <div>
                                    <label for="avatarFile" class="cursor-pointer bg-white py-2 px-3 border border-gray-300 rounded-lg shadow-sm text-sm leading-4 font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200">
                                        <span>Thay đổi ảnh</span>
                                        <input id="avatarFile" name="avatarFile" type="file" class="sr-only" accept="image/*">
                                    </label>
                                    <p class="text-xs text-gray-500 mt-2">PNG, JPG, GIF tối đa 5MB</p>
                                </div>
                            </div>
                        </div>

                        <!-- Status -->
                        <div class="form-group">
                            <label asp-for="TrangThai" class="block text-sm font-medium text-gray-700 mb-2">
                                <span class="flex items-center">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                    </svg>
                                    Trạng thái
                                </span>
                            </label>
                            <select asp-for="TrangThai" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors duration-200">
                                <option value="ACTIVE">🟢 Hoạt động</option>
                                <option value="INACTIVE">🔴 Không hoạt động</option>
                            </select>
                            <span asp-validation-for="TrangThai" class="text-red-500 text-sm mt-1 block"></span>
                        </div>

                        <!-- System Information (Read-only) -->
                        <div class="space-y-4 bg-gray-50 p-6 rounded-lg">
                            <h4 class="text-sm font-medium text-gray-900 flex items-center">
                                <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
                                </svg>
                                Thông tin hệ thống
                            </h4>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <div class="bg-white p-3 rounded border border-gray-200">
                                    <label class="block text-sm font-medium text-gray-500 mb-1">Ngày tham gia</label>
                                    <p class="text-sm text-gray-900">@Model.NgayThamGia.ToString("dd/MM/yyyy")</p>
                                </div>
                                <div class="bg-white p-3 rounded border border-gray-200">
                                    <label class="block text-sm font-medium text-gray-500 mb-1">Ngày tạo</label>
                                    <p class="text-sm text-gray-900">@Model.NgayTao.ToString("dd/MM/yyyy HH:mm")</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Password Reset Section (Admin Only) -->
                @if (User.IsInRole("Admin"))
                {
                    <div class="mt-8 pt-6 border-t border-gray-200">
                        <div class="bg-red-50 border border-red-200 rounded-lg p-4">
                            <div class="flex items-center mb-3">
                                <svg class="w-5 h-5 text-red-600 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                                </svg>
                                <h3 class="text-lg font-medium text-red-900">🔑 Đặt lại mật khẩu</h3>
                            </div>
                            <p class="text-sm text-red-700 mb-4">
                                Chức năng này sẽ tạo mật khẩu mới ngẫu nhiên và gửi email thông báo cho người dùng.
                            </p>
                            <div class="flex items-center space-x-4">
                                <button type="button" id="resetPasswordBtn" onclick="resetUserPassword(@Model.NguoiDungId)"
                                        class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
                                    </svg>
                                    Đặt lại mật khẩu
                                </button>
                                <span class="text-xs text-red-600">
                                    ⚠️ Hành động này không thể hoàn tác
                                </span>
                            </div>
                        </div>
                    </div>
                }

                <!-- Action Buttons -->
                <div class="flex gap-4 mt-8 pt-6 border-t border-gray-200">
                    <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-md font-medium transition-colors duration-200 flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                        Lưu thay đổi
                    </button>
                    <a href="@Url.Action("Details", new { id = Model.NguoiDungId })" class="bg-gray-300 hover:bg-gray-400 text-gray-700 px-6 py-2 rounded-md font-medium transition-colors duration-200 flex items-center">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                        Hủy
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Avatar preview
            const avatarInput = document.getElementById('avatarFile');
            const avatarPreview = document.getElementById('avatarPreview');
            
            avatarInput.addEventListener('change', function(e) {
                const file = e.target.files[0];
                if (file && file.type.startsWith('image/')) {
                    const reader = new FileReader();
                    reader.onload = function(e) {
                        avatarPreview.innerHTML = `<img src="${e.target.result}" class="h-24 w-24 rounded-lg object-cover" alt="Preview">`;
                    };
                    reader.readAsDataURL(file);
                }
            });

            // Form validation
            const form = document.getElementById('editUserForm');
            form.addEventListener('submit', function(e) {
                // Additional client-side validation if needed
            });

            // Show validation errors from server
            const serverErrors = document.querySelectorAll('.field-validation-error');
            if (serverErrors.length > 0) {
                document.getElementById('validationSummary').style.display = 'block';
            }
        });

        // Reset Password Function
        async function resetUserPassword(userId) {
            if (!confirm('⚠️ Bạn có chắc chắn muốn đặt lại mật khẩu cho người dùng này?\n\nMật khẩu mới sẽ được tạo ngẫu nhiên và gửi qua email.')) {
                return;
            }

            const button = document.getElementById('resetPasswordBtn');
            const originalText = button.innerHTML;

            try {
                // Disable button and show loading
                button.disabled = true;
                button.innerHTML = `
                    <div class="animate-spin rounded-full h-4 w-4 border-b-2 border-white mr-2"></div>
                    Đang đặt lại...
                `;

                // Get CSRF token
                const token = document.querySelector('input[name="__RequestVerificationToken"]').value;

                const response = await fetch('@Url.Action("ResetPassword", "User")', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/x-www-form-urlencoded',
                        'RequestVerificationToken': token
                    },
                    body: `userId=${userId}&__RequestVerificationToken=${encodeURIComponent(token)}`
                });

                const result = await response.json();

                if (result.success) {
                    // Show success notification
                    showNotification('success', result.message);

                    // Show detailed modal
                    showPasswordResetModal(result);
                } else {
                    showNotification('error', result.message || 'Không thể đặt lại mật khẩu');
                }
            } catch (error) {
                console.error('Error:', error);
                showNotification('error', 'Có lỗi xảy ra khi đặt lại mật khẩu');
            } finally {
                // Restore button
                button.disabled = false;
                button.innerHTML = originalText;
            }
        }

        // Show notification
        function showNotification(type, message) {
            const notification = document.createElement('div');
            notification.className = `fixed top-4 right-4 z-50 p-4 rounded-md shadow-lg ${
                type === 'success' ? 'bg-green-100 border border-green-400 text-green-700' : 'bg-red-100 border border-red-400 text-red-700'
            }`;
            notification.innerHTML = `
                <div class="flex items-center">
                    <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
                        ${type === 'success'
                            ? '<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>'
                            : '<path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"></path>'
                        }
                    </svg>
                    <span>${message}</span>
                    <button onclick="this.parentElement.parentElement.remove()" class="ml-4 text-gray-400 hover:text-gray-600">
                        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                        </svg>
                    </button>
                </div>
            `;
            document.body.appendChild(notification);

            // Auto remove after 5 seconds
            setTimeout(() => {
                if (notification.parentElement) {
                    notification.remove();
                }
            }, 5000);
        }

        // Show password reset modal
        function showPasswordResetModal(result) {
            const modal = document.createElement('div');
            modal.className = 'fixed inset-0 bg-gray-600 bg-opacity-50 flex items-center justify-center z-50';
            modal.innerHTML = `
                <div class="bg-white rounded-lg p-6 max-w-md w-full mx-4">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-lg font-medium text-gray-900">🔑 Mật khẩu đã được đặt lại</h3>
                        <button onclick="this.closest('.fixed').remove()" class="text-gray-400 hover:text-gray-600">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                            </svg>
                        </button>
                    </div>

                    <div class="mb-4">
                        <div class="bg-green-50 border border-green-200 rounded-md p-4">
                            <div class="flex">
                                <div class="flex-shrink-0">
                                    <svg class="h-5 w-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                                    </svg>
                                </div>
                                <div class="ml-3">
                                    <p class="text-sm text-green-800">
                                        <strong>Thành công!</strong> ${result.message}
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>

                    ${result.emailError ? `
                        <div class="mb-4">
                            <div class="bg-yellow-50 border border-yellow-200 rounded-md p-4">
                                <div class="flex">
                                    <div class="flex-shrink-0">
                                        <svg class="h-5 w-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                                            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
                                        </svg>
                                    </div>
                                    <div class="ml-3">
                                        <p class="text-sm text-yellow-800">
                                            <strong>Cảnh báo:</strong> ${result.emailError}
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    ` : ''}

                    ${result.username ? `
                        <div class="mb-4">
                            <label class="block text-sm font-medium text-gray-700 mb-2">Tên đăng nhập:</label>
                            <div class="flex">
                                <input type="text" value="${result.username}" readonly
                                       class="flex-1 px-3 py-2 border border-gray-300 rounded-l-md bg-gray-50 text-gray-900 font-mono">
                                <button onclick="copyToClipboard('${result.username}')"
                                        class="px-3 py-2 bg-gray-600 text-white rounded-r-md hover:bg-gray-700">
                                    📋 Copy
                                </button>
                            </div>
                        </div>
                    ` : ''}

                    ${result.newPassword ? `
                        <div class="mb-4">
                            <label class="block text-sm font-medium text-gray-700 mb-2">Mật khẩu mới:</label>
                            <div class="flex">
                                <input type="text" value="${result.newPassword}" readonly
                                       class="flex-1 px-3 py-2 border border-gray-300 rounded-l-md bg-gray-50 text-gray-900 font-mono">
                                <button onclick="copyToClipboard('${result.newPassword}')"
                                        class="px-3 py-2 bg-blue-600 text-white rounded-r-md hover:bg-blue-700">
                                    📋 Copy
                                </button>
                            </div>
                            <p class="text-xs text-gray-500 mt-1">
                                ${result.emailSent ? 'Mật khẩu này đã được gửi qua email cho người dùng' : 'Vui lòng thông báo mật khẩu này cho người dùng'}
                            </p>
                        </div>
                    ` : ''}

                    <div class="flex justify-end">
                        <button onclick="this.closest('.fixed').remove()"
                                class="px-4 py-2 bg-gray-300 text-gray-700 rounded-md hover:bg-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-500">
                            Đóng
                        </button>
                    </div>
                </div>
            `;
            document.body.appendChild(modal);
        }

        // Copy to clipboard function
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => {
                showNotification('success', 'Đã copy mật khẩu vào clipboard');
            }).catch(() => {
                showNotification('error', 'Không thể copy mật khẩu');
            });
        }
    </script>
}
